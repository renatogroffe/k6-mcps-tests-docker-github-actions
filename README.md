# k6-mcps-tests-docker-github-actions
Exemplo de implementação de testes de validação com k6 para MCP Servers acionados via docker run (com um exemplo de aplicação .NET 10 containerizada) e que inclui execução automatizada a partir de um workflow do GitHub Actions. Inclui o build do executável do k6 com a extensão para MCPs via container.
